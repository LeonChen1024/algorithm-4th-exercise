package edu.princeton.cs.exercise.chapter3_2;

/**
 * 3.2.30 Order check. Write a recursive method isOrdered() that takes a Node and two
 * keys min and max as arguments and returns true if all the keys in the tree are between
 * min and max; min and max are indeed the smallest and largest keys in the tree,
 * respectively; and the BST ordering property holds for all keys in the tree; false
 * otherwise.
 * <p>
 * 顺序校验.编写一个递归方法 isOrdered() 接收一个节点和两个键 min 和 max 作为参数并且当树中所有的键都在
 * min 和 max 之间的时候返回 true;min 和 max 实际上就是树中最小和最大的键;并且 BST 排序属性试用树中所有
 * 的键;否则返回 false
 *
 * @author LeonChen
 * @since 4/25/21
 */
class E3_2_30 {

    /**
     * @formatter:off
     * 参考官方 BST 中的 isBST(Node x, Key min, Key max) 方法
     * @formatter:on
     */
    public static void main(String[] args) {

    }


}
