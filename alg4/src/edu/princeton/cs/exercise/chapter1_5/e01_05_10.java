package edu.princeton.cs.exercise.chapter1_5;

/**
 * 1.5.10 In the weighted quick-union algorithm, suppose that we set id[find(p)] to q instead of to
 * id[find(q)]. Would the resulting algorithm be correct?
 *
 * <p>Answer : Yes, but it would increase the tree height, so the performance guarantee would be
 * invalid.
 *
 * <p>在 weighted quick-union 算法中,假设我们设置 id[find(p)] 为 q 而不是 id[find(q)].这个算法的结果还正确吗?
 *
 * <p>答案: 正确,但是它会增加树的高度,所以将无法保障性能.
 *
 * @author LeonChen
 * @since 4/19/20
 */
class e01_05_10 {

  /** */
  public static void main(String[] args) {}
}
